/*Array.array.forEach(callbackfunction){  
});
callback function:A callback function passed as an argument to another function*/
//it checks for each value in an array

let arr=[1,2,3,4,5];
    arr.forEach((val)=>
    {
        console.log(val)
    })
